# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

DinMeterDock is a desktop Dock controller based on the M5DinMeter device (M5Stack ESP32-based hardware). The project is in its early stages.

## Build System

The `.gitignore` is configured for C/C++ embedded development with CMake support. Expected build toolchain:

- **Platform**: ESP32 (M5DinMeter / M5Stack)
- **Framework**: Likely Arduino (ESP32 Arduino core) or ESP-IDF
- **Build tool**: PlatformIO (`pio`) or Arduino IDE or CMake + ESP-IDF

## Repository State

This repository is newly initialized (single initial commit). Source code has not yet been added.
